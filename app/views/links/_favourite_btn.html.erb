<%= link_to toggle_favourite_path(id: link.id), method: 'post', remote: true do%>
  <i class="fa fa-heart favourite_btn" title= "<%= favourite_action_tooltip(link)%>" id=<%= 'tomato' if is_favourite_link?(link)  %> ></i>
<%end%>